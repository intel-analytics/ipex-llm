name: stable-diffusion-win
on:
  pull_request:
    branchs: [ main ]
  schedule:
    - cron: '0 3 * * *' # GMT time, 3:00 GMT == 3:00 China Everyday
  workflow_dispatch:

jobs:
  stable-diffusion-win:
    runs-on: [self-hosted, Gondolin, ubuntu-20.04-lts]
    steps:
    - name: "stable-diffusion-win"
#      env:
#        FTP_URII_PSD: ${{ secrets.FTP_PASSWORD }}
      run: |
        export FTP_URI=ftp:1234qwer//bigdl:@10.112.231.51:8821
        wget  $FTP_URI/stable-diffusion/ui-test-20230607.log
        cat ui-test-20230607.log
        rm ui-test-20230607.log
