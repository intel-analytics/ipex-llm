apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: friesian-serving-resources
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
  storageClassName: managed-nfs-storage
