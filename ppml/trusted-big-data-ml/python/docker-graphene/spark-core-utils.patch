521c521,522
<     FileUtil.chmod(targetFile.getAbsolutePath, "a+x")
---
>     println("######################### fork chmod is forbidden !!!" + targetFile.getAbsolutePath)
>     // FileUtil.chmod(targetFile.getAbsolutePath, "a+x")
1221a1223
>     println("###################### processbuilder in utils.executeCommand, " + command)
